# Build artifacts
server/jmessage
server/jmessage.exe
server/data/
*.prof

# Frontend
web/node_modules/
web/dist/
web/.env.local
web/.env.*.local

# Embedded frontend copy (populated by the Docker build; keep only the
# .gitkeep placeholder so `go build` works without it)
server/internal/webui/dist/*
!server/internal/webui/dist/.gitkeep

# IDE & editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
*.sublime-project
*.sublime-workspace

# OS
Thumbs.db
.DS_Store

# Misc
*.log
.env
